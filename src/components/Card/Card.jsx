import React from 'react';
import classes from './Card.module.scss';

export const Card = () => {
    return (
        <div className={classes.card}>
            <h1>Card</h1>
        </div>
    );
};
